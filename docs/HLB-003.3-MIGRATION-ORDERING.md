# HLB-003.3 — Privacy Migration Ordering / Self-test Isolation

Fixes the HLB-003.2 false PASS:
- migrate before install/self-test;
- recover the last real delivery route from historical evidence even if RouteStore was overwritten;
- write hash-only pre-redaction attestation;
- sanitize primary trace in place;
- delete raw historical trace backups after attestation;
- byte-scan the entire HLB state directory for recovered route identity;
- restore production RouteStore to the recovered real route;
- reject synthetic routes in RouteStore;
- run self-test with route persistence disabled.

No provider message is sent.
