# HLB-003.5 — Canonical Representation Boundary

**Status: FINAL ACCEPTANCE PASS — HLB-003 COMPLETE**

HLB-003.5 is the final closure of the HLB-003 Contact Bridge milestone.

Operational records must never contain Python/runtime implementation representations such as:

- `SessionSource(`
- `Platform.FEISHU`
- `<Platform.*>`
- `object at 0x...`

All trace and ContactStore writes pass through one canonical representation boundary. Known platform representations normalize to primitive platform strings (for example `feishu`); unknown runtime objects fail closed to `[REDACTED_RUNTIME_OBJECT]`.

Historical trace/contact DB rows receive an in-place migration with SQLite secure-delete, VACUUM and WAL truncate.

Release/acceptance invariants:

- quiesce writers before migration/final byte scan;
- restore service state on EXIT on both success and failure;
- self-test cannot overwrite the production RouteStore;
- exact route exists only in the private RouteStore, mode `0600`;
- ordinary trace contains no raw chat ID or canonical target;
- ContactStore/WAL contains no raw route;
- no Gateway reload is required for the closure migration;
- no provider send is performed or required during this closure migration; preserve the prior successful HLB-003.1 delivery evidence.

## Final acceptance evidence

GB10 independent readback:

```text
Hotfix install: PASS
HLB tests: PASS
count = 48

Trace canonicalization = PASS
ContactStore canonicalization = PASS
Historical migration = PASS

SessionSource( count = 0
Platform.FEISHU count = 0
<Platform. count = 0
object at 0x count = 0

ordinary trace raw chat_id = NO
ordinary trace canonical target = NO

Contact DB/WAL raw route = NO
Contact DB/WAL SessionSource repr = NO
Contact DB/WAL Platform repr = NO
WAL size = 0

RouteStore platform = feishu
chat_id_present = true
mode = 0600
self-test overwrite = NO

cognition service state restored = YES
contact service state restored = YES

delivery_enabled = false
SafeStop = NO
Doctor = HEALTHY
new provider send during HLB-003.5 = NO
```

Canonical acceptance markers:

```text
CANONICAL_REPRESENTATION_BOUNDARY=PASS
CONTACT_DB_REPRESENTATION_CLEAN=PASS
FINAL_REPRESENTATION_BYTE_READBACK=PASS
HLB0035_CANONICAL_ACCEPTANCE=PASS
HLB0035_INSTALL=PASS
```

**Result: HLB-003 Contact Bridge = COMPLETE.**
